import React from 'react';

const Landing = () => {
    return(
        <div style={{ textAlign: 'center', color: '#888'}}>
            <h1>Emaily</h1>
            Easy way to collect surveys from users.
        </div>
    );
}

export default Landing;